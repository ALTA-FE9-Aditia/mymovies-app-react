import { createContext } from "react";

export const ThemeContext = createContext({});

/*
creatrContext -- digunakan untuk membuat sebuah wadah yang diletakan
di hiraraki paling atas dari sebuah komponen  */
